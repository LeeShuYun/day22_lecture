package dev.leeshuyun.day22_lecture;

import dev.leeshuyun.day22_lecture.repo.EmployeeRepo;
import dev.leeshuyun.day22_lecture.model.Employee;
/* 
 * 
 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.boot.test.context.SpringBootTest;
 import org.springframework.test.context.junit.jupiter.SpringExtension;
 
 @ExtendWith(SpringExtension.class)
 @SpringBootTest
 public class EmployeeRepoTest {
     @Autowired
     EmployeeRepo empRepo;
     
     @Test 
     public void retrieveAllEmployees(){
         List<Employees> empList = empRepo.retrieveEmployeeList();
        empList.forEach((emp) -> System.out.print(emp));
        assertTrue(empList.size() > 0);
    }
}

         */